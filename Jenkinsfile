pipeline {
    agent {
        dockerfile true
    }
    stages {
        stage('Docker') {
            steps {
                sh 'node --version'
                sh 'echo "e pp"'
            }
        }
    }
}